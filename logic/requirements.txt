# Voice commands (stretch goal) only -- notifications/escalation/occupant-watch/
# gestures need nothing beyond the standard library.
sounddevice>=0.5
SpeechRecognition>=3.10
pyaudio>=0.2.14
win10toast>=0.34  # occupant-left-behind Windows notifications
